public class Tasksheet113 {
    public static void main(String[] args) {
        // Declare primitive variables
       byte zero = 0;      // byte value
       char H ='H' ;      // char value
       Short three =3;   // short value
       int one = 1;     // int value
      float twoPointZero= 2.0f;  // float value
      boolean isTrue= true;  //boolean value 
     // Concatenate the primitives into a String
    String output=H+""+three+one+one+zero + "w" + zero + "r" + one + "d" + twoPointZero+""+isTrue;
     // Print the concatenated string
System.out.println(output);
    }
}